Как использовать: распаковать архив в корень репозитория (пути внутри
архива уже совпадают со структурой репо) — существующие файлы будут
перезаписаны, новые появятся на своих местах. После распаковки — рестарт
бота (systemctl restart trustpanel-bot, либо через /opt/trustpanel/venv...
как у вас настроено).

Отмечены [НОВЫЙ] — файла раньше не было, остальные — изменённые.

--- v2.8 (последний заход) ---
bot/keyboards.py, bot/handlers/broadcast.py, bot/handlers/settings.py:
    "📢 Рассылка" -> "📢 Обращения", туда же переехала "💬 Переписки" из
    Настроек.
bot/handlers/auto_renewal_review.py:
    Теперь видно реальный текущий URL прокси для Gemini (не только
    вкл/выкл) — и в статусе автопродления, и в настройках условий.
---

CHANGELOG.md
    Полная история изменений, актуальная запись — v2.7 сверху.

follower_issuance.py
    FREE_EXTRA_LINKS = 2 (бесплатный лимит доп. ссылок).

bot/bot.py
    Регистрация нового роутера settings.py.

bot/keyboards.py
    main_menu: -3 кнопки, +"⚙️ Настройки". client_menu: "Инструкция" ->
    "Инструкции". instructions_menu_kb: +3-й пункт "Как пользоваться
    ботом". +bot_usage_button_kb().

bot/handlers/settings.py                                          [НОВЫЙ]
    Точка входа "⚙️ Настройки" (сортировка/триалы/автопродление/sync/
    проверка привязок — переиспользует существующую логику из других
    файлов) + браузер "💬 Переписки" (список/просмотр/удаление).

bot/handlers/sorting.py
    Вход теперь только через колбэк settings:sorting (убран прямой
    F.text-хендлер с главного меню).

bot/handlers/sync_deploy.py
    Sync users переведён на колбэк settings:sync. Деплой не тронут.

bot/handlers/broadcast.py
    Кнопка "Проверить привязки" убрана из меню Рассылки (сама функция
    осталась как есть — только тех, у кого telegram_id уже есть).

bot/handlers/database.py
    Кнопка "Управление триалами" убрана (переехала в Настройки, тот же
    callback db:trials).

bot/handlers/auto_renewal_review.py
    Убрана кнопка "✅ Подтвердить" из карточек автопродления. Добавлена
    "ℹ️ Как это работает" (короткий статус отдельно от полного текста).
    Добавлено поле "🌐 Прокси-адрес" в настройках. Вход также через
    колбэк settings:autoren.

bot/handlers/client_menu.py
    "Инструкция" -> "Инструкции" (3 пункта). Кнопка "Как пользоваться
    ботом" под платформенными инструкциями (howto:ios/howto:android).

bot/handlers/start.py
    /start переведён на CommandStart() с разбором deep-link. Обработка
    инвайт-ссылок (_handle_invite). Автоотправка гайда после триала и
    после привязки картой.

bot/handlers/add_user.py
    Кнопка "🔗 Инвайт-ссылка" на карточках (одиночное и массовое
    добавление) + общий колбэк-хендлер invite:gen:{username}.

bot/handlers/list_users.py
    Кнопка "🔗 Инвайт-ссылка" в карточке клиента (только если telegram_id
    ещё не привязан).

bot/handlers/feedback.py
    Запись входящих/исходящих сообщений в core/messages.py (чеки не
    пишутся — у них свой поток).

core/auto_renewal.py
    _apply_and_request_review: pending_request чистится сразу (не по
    клику админа) — чинит блокировку доп. ссылок. Данные отката — в
    last_auto_renewal. +get/set_gemini_proxy_url_override().

core/gemini_client.py
    timeout=60. Ретраи (до 3, с паузой 2с/4с) на 429/500/502/503/504.
    proxy_url()/proxy_configured() учитывают live-override.

core/instructions.py
    +render_bot_usage_instructions() — текст "Как пользоваться ботом".

core/db.py
    +get_user_by_invite_token().

core/invite.py                                                     [НОВЫЙ]
    generate_invite_token() / build_invite_link() — бессрочная-до-
    использования инвайт-ссылка.

core/paths.py
    +MESSAGES_PATH, добавлен в BACKUP_FILES.

core/messages.py                                                   [НОВЫЙ]
    Хранилище переписки (бессрочно, без авто-удаления) + список бесед/
    получение истории/удаление одной беседы целиком.

core/service.py
    restart_trusttunnel(): опрос systemctl is-active вместо мгновенного
    доверия к возврату restart.

core/generator.py
    generate_link(): до 3 попыток при ошибке/пустом ответе бинарника.

--- v2.9 (последний заход) ---
bot/handlers/extra_links.py:
    Карточка заявки показывает текущее кол-во + запрошенное. Новая
    кнопка "💚 Без наценки" (approve_free) рядом с "✅ Выдать".
core/payment.py:
    +BASE_PRICE_PER_MONTH, +EXTRA_LINK_SURCHARGE (30₽), +calc_monthly_price().
bot/handlers/client_menu.py:
    "💳 Реквизиты для оплаты" считает и показывает итоговую сумму с
    учётом доп. ссылок сверх бесплатного лимита.
---

--- v3.0 (последний заход) ---
bot/handlers/extra_links.py:
    Кнопка "💬 Открыть чат в Telegram" в карточке заявки на доп. ссылки.
core/auto_renewal.py:
    evaluate_receipt_extraction() принимает rate клиента вместо
    фиксированной min_amount.
bot/handlers/receipt.py, bot/handlers/feedback.py, max_bot/handlers/receipt.py:
    Ставка клиента (calc_monthly_price) фиксируется в pending_request в
    момент подтверждения отправки чека ("rate_at_submission").
---

--- v3.1 (последний заход) ---
bot/handlers/extra_links.py:
    Экран выбора доп. ссылок — подробное объяснение + цена прямо на
    кнопках (адаптируется под остаток бесплатного лимита).
---

--- v3.2 (последний заход) ---
core/payment.py:
    +get_last_renewal_rate, +calc_rate_gap, +calc_recalculated_expiry,
    +calc_topup_amount.
core/auto_renewal.py, bot/handlers/receipt.py:
    last_renewal_rate трекается при каждом реальном продлении (авто и
    ручном), откатывается при "🚫 Отключить".
bot/handlers/auto_renewal_review.py:
    Откат ("🚫 Отключить") восстанавливает last_renewal_rate.
bot/handlers/client_menu.py:
    Кнопки "🔄 Пересчитать срок" / "💰 Доплатить сейчас" на экране
    "💳 Реквизиты для оплаты", показываются только если есть разрыв
    ставки. Доплата — отдельная заявка админу (rtopup:).
bot/handlers/extra_links.py:
    Явная формулировка "со следующего продления" в уведомлении клиенту.
bot/states.py:
    +RateTopup.waiting_receipt.
---

--- v3.3 (последний заход) ---
bot/handlers/client_menu.py:
    Кнопки "Доплатить"/"Пересчитать" показываются по отдельности, в
    зависимости от остатка оплаченного срока (< месяца / >= месяца).
bot/handlers/extra_links.py:
    Полная блокировка выдачи доп. ссылок, пока висит rate_topup.
core/auto_renewal.py:
    +try_auto_verify_topup() — автопроверка доплаты через Gemini.
---

--- v3.4 (последний заход) ---
bot/handlers/extra_links.py:
    Кнопки выбора количества подписаны диапазоном устройств (1-2, 3-4,
    5-6, 7-8), а не числом ссылок.
---

--- v3.5 (последний заход) ---
bot/handlers/extra_links.py:
    Переписан целиком — оплата за месяц по новой ставке вместо
    прорейтинга, автовыдача через Gemini, тексты про устройства,
    компактные горизонтальные кнопки.
core/payment.py:
    +calc_monthly_price_with_additional().
core/auto_renewal.py:
    +try_auto_verify_extra_links_payment(), +_issue_followers_sync().
bot/states.py:
    +ExtraLinksPayment.waiting_receipt.
bot/handlers/client_menu.py:
    Тексты переписаны на "устройства" вместо "ссылки".
---

--- v3.6 (последний заход) ---
bot/handlers/extra_links.py:
    Фикс: к оплате идёт только маржинальная сумма (не весь новый месяц).
    Убрано продление даты. Добавлена ветка "согласие вместо оплаты" для
    короткого остатка срока (≤4 дня).
core/auto_renewal.py:
    try_auto_verify_extra_links_payment — та же правка (маржинальная
    сумма, без продления даты).
core/payment.py:
    +PAYMENT_LINK, +PAYMENT_RECIPIENT, +SHORT_REMAINING_THRESHOLD_DAYS.
---

--- v3.7 (последний заход) ---
bot/handlers/extra_links.py:
    Фикс подписи кнопок выбора устройств — показывает реальный диапазон
    с учётом уже имеющихся устройств, а не абсолютную нумерацию с нуля.
---

--- v3.8 (последний заход) ---
bot/handlers/extra_links.py:
    Откат v3.7: кнопки снова относительные (1-2, 3-4...). Добавлена
    строка "Вы уже получили N ссылки на M устройств" перед кнопками.
---

--- v3.9 (последний заход) ---
bot/config.py, bot/bot.py:
    Dispatcher (dp) перенесён в config.py, чтобы хендлеры могли к нему
    обращаться без циклического импорта.
bot/handlers/settings.py:
    +"🔓 Закрыть висящие заявки" — список зависших pending_request,
    очистка заявки + сброс FSM-состояния клиента point-and-click.
---

--- v3.10 (последний заход) ---
core/payment.py:
    PAYMENT_INFO/ACCESS_EXPIRED_MESSAGE -> render_payment_info() /
    render_access_expired_message() / render_payment_info_for_user() —
    показывают реальную ставку клиента, одна строка вместо противоречия.
bot/handlers/client_menu.py, max_bot/handlers/client_menu.py,
services/cleanup.py, services/post_disable_reminders.py:
    Все клиентские сообщения о реквизитах/истёкшем доступе переведены на
    новые функции.
---
